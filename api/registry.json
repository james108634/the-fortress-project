# Web
cd /root
docker build -t fortress-web -f Dockerfile .
docker rm -f fortress-web || true
docker run -d --name fortress-web -p 3000:3000 fortress-web

# API
cd /root/api
docker build -t fortress-api -f Dockerfile .
docker rm -f fortress-api || true
docker run -d --name fortress-api -p 4000:4000 fortress-api

# Verify
curl http://localhost:4000/api/health
curl http://localhost:3000
